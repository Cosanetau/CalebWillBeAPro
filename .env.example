# Shared access word. Leave unset to create it in the app on first visit.
ACCESS_WORD=

# Optional Upstash/Vercel KV so Caleb and the nutritionist share one live store.
KV_REST_API_URL=
KV_REST_API_TOKEN=
